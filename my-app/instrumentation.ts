/**
 * Instrumentation hook — Next.js loads this file automatically.
 * Empty export is the cleanest no-op: no imports, no runtime cost.
 * If Sentry / OpenTelemetry integration is needed later, add it here.
 */
export function register() {
  // Intentionally empty.
}
